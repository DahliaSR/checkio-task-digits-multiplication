"use strict";

function checkio(data) {
    return data;
}

var assert = require('assert');

if (!global.is_checking) {
    assert.equal(checkio(123405), 120, "1st");
    assert.equal(checkio(999), 729, "2nd");
    assert.equal(checkio(1000), 1, "3rd");
    assert.equal(checkio(1111), 1, "4th");
    console.log("Coding complete? Click 'Check' to review your tests and earn cool rewards!");
}
